name: Flask API CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t mindeaseModel .

      - name: Run app and tests
        run: |
          docker run -d -p 5000:5000 --name app mindeaseModel
          sleep 5
          docker exec app python test.py
